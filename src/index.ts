/**
 * @axhy/shared — single source of truth for state machines, types, and utilities.
 *
 * Imported by both axhy-admin (Next.js server) and axhy-v2-b2b (Expo client).
 * When this package version changes, both consumers MUST move together.
 */

export * from "./state-machines";
export * from "./utils";
export * from "./types";
export * from "./ai";
